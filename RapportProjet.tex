\documentclass[12pt]{article}
\usepackage{tabularx}
\usepackage[table]{xcolor}
\usepackage{hyperref}
\usepackage{float}
\usepackage{graphicx}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{lmodern}
\usepackage{amsmath, amssymb}
\usepackage{graphicx}
\usepackage[a4paper,margin=1.5cm]{geometry}

\title{Integration Project -- Model Predictive Control (MPC)\\[0.3cm]
Modeling and Control of a Transport Network}
\author{Clément Milcent, Kevin Armel Wassa, Mathis Poteau}
\date{January 2025}

\begin{document}

\maketitle

\newpage
\tableofcontents
\newpage

\section{Introduction}

This project focuses on the modeling and optimal control of a transport network represented as a directed graph.  
The objective is to describe the evolution of the volumes stored in the different components of the network and to formulate a Model Predictive Control (MPC) problem in order to optimize the global behavior of the system over a finite time horizon.

First, a mathematical model of the network dynamics is introduced, together with the associated physical constraints.  
Then, a convex relaxation of the model is proposed in order to make the optimal control problem tractable.  
Finally, a finite-horizon optimal control problem is formulated.

\section{Network Model}

The transport network is represented by a directed graph $G = (V,E)$ composed of two types of nodes:
\begin{itemize}
    \item $\mathcal{T}$: set of \textbf{tanks} (reservoirs),
    \item $\mathcal{X}$: set of \textbf{transport cells} (tunnels).
\end{itemize}

The set of vertices is given by $V = \mathcal{T} \cup \mathcal{X}$, with $\mathcal{T} \cap \mathcal{X} = \emptyset$.

Time is discretized with a constant sampling period $h > 0$:
\[
t_k = k h, \quad k = 0,1,\dots,K.
\]

\subsection{State and control variables}

For each discrete time step $k$, the following variables are defined:
\begin{itemize}
    \item $x_T(k)$: water volume stored in tank $T \in \mathcal{T}$,
    \item $x_i(k)$: water volume stored in cell $i \in \mathcal{X}$,
    \item $u_T(k) \in [0,1]$: valve opening control associated with tank $T$,
    \item $f_{ij}(k) \ge 0$: flow from node $i$ to node $j$,
    \item $\mu_i(k)$: outflow leaving the network from terminal cells.
\end{itemize}

\subsection{Demand and supply functions}

For each transport cell $i \in \mathcal{X}$, demand and supply functions are introduced.

\paragraph{Demand function}

The demand function is defined as:
\[
d_i(x_i(k)) = \gamma_i \min\left( \frac{v_i}{l_i} x_i(k),\; C_i \right),
\]
where $v_i$ denotes the velocity, $l_i$ the length of the cell, $C_i$ the maximum capacity, and $\gamma_i \in [0,1]$ is a control parameter used to regulate the flow.

\paragraph{Supply function}

The supply function is given by:
\[
s_i(x_i(k)) = \max\left(c_i - w_i x_i(k),\; 0\right),
\]
where $c_i$ is a capacity constant and $w_i$ is a saturation coefficient.

\subsection{Flow definitions}

\paragraph{Cell-to-cell flow}

For two transport cells $i,j \in \mathcal{X}$, the flow is defined as:
\[
f_{ij}(k) = \min\left(d_i(x_i(k)),\; s_j(x_j(k))\right).
\]

\paragraph{Many cells to a single cell flow}
We have $ m \in \mathcal{X}$ cells that all lead to a single cell $j \in \mathcal{X}$. The flow is defined as : 
\[
    \forall i \in [1,m],\ f_{ij} = min\ (\beta d_i(x_i(k)),s_j(x_j(k)))
\]


\paragraph{Tank-to-cell flow}

For a tank $T \in \mathcal{T}$ and a cell $j \in \mathcal{X}$:
\[
f_{Tj}(k) = \min\left(u_T(k)\, x_T(k),\; s_j(x_j(k))\right).
\]

\paragraph{Outflow from the network}

If a cell $i$ has no successor, the outflow leaving the network is:
\[
\mu_i(k) = d_i(x_i(k)).
\]

\paragraph{No return to tanks}

Flows from transport cells back to tanks are forbidden:
\[
f_{jT}(k) = 0, \quad \forall j \in \mathcal{X},\ \forall T \in \mathcal{T}.
\]

\subsection{Discrete-time system dynamics}

\paragraph{Tank dynamics}

The evolution of the tank volumes is described by:
\[
x_T(k+1) = x_T(k) - h \sum_{j:\,T \to j} f_{Tj}(k).
\]

\paragraph{Cell dynamics}

The dynamics of the transport cells are given by:
\[
x_i(k+1) = x_i(k)
+ h \left(
\sum_{j:\,j \to i} f_{ji}(k)
- \sum_{m:\,i \to m} f_{im}(k)
- \mu_i(k)
\right),
\quad i \in \mathcal{X}.
\]

\subsection{Physical constraints}

The system is subject to the following constraints:
\[
\begin{cases}
x_i(k) \ge 0, \quad x_T(k) \ge 0, \\
f_{ij}(k) \ge 0, \\
0 \le u_T(k) \le x_T(k).
\end{cases}
\]

When we get $m \in \mathcal{X}$ cells to a single cell $j \in \mathcal{X}$, we add these constraints : 
\[
    \sum_{i=1}^{m}{f_{ij} \le s_j}
\] 


\section{Open-Loop Analysis of a Simple Network}
\subsection{Network Description: Line Graph --- Two Tanks and Six Cells}
\begin{figure}[H]
    \centering
    \includegraphics[width=0.7\textwidth]{graph_2Tanks_6cells.png}
    \caption{Network with two Tanks and six Cells}
    \label{fig:simpleNetworkFig}
\end{figure}

First we modeled a simple line graph network with 6 cells and two tanks plugged respectively on the first and the fourth cell as we can see in figure~\ref{fig:simpleNetworkFig}. Each cell have a 500 meters length and a capacity of 1000 vehicles/h. Each vehicle move at 70 km/h in the cells. The two tanks are initially filled with 100 vehicles.


\subsection{Open-Loop Simulation Setup and Results}
To empty the tanks, we will try two naive open-loop strategies. First we will try to empty both tanks at the same time and then we will totally empty one tank and then proceed to empty the other one (figure~\ref{fig:simpleNetwork_tankEmptying}). 
\begin{figure}[H]
    \centering
    \includegraphics[width=0.7\textwidth]{tank_states_simulateur_OL.png}
    \includegraphics[width=0.7\textwidth]{tank_states_simulateur_OL_command_differe.png}
    \caption{Different ways to empty the tanks : both emptying at the same time or totally empty one and then the other one}
    \label{fig:simpleNetwork_tankEmptying}
\end{figure}
When the two tanks are being emptied at the same time (figure~\ref{fig:simpleNetwork_resultBothTanksEmpt}), there some stress on cell X4 (it peaks at 85 vehicles around 400s). This can be explained by the addition of vehicles coming from the first and second tanks, which accumulate at cell 4. 
When emptying the tanks one after the other (figure~\ref{fig:simpleNetwork_resultTankOneThenAnother}) (first tank 2, then tank 1), we notice that we never exceed twenty vehicles, regardless of the cell. Even if it takes a little longer in this configuration, we notice that there is less stress on the cells.
\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{cell_states_simulateur_OL.png}
    \caption{Results when both tanks are emptied at the same time}
    \label{fig:simpleNetwork_resultBothTanksEmpt}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{cell_states_simulateur_OL_command_differe.png}
    \caption{results when the tanks are emptied one after the other}
    \label{fig:simpleNetwork_resultTankOneThenAnother}
\end{figure}
\subsection{Discussion}



\section{Model Predictive Control Formulation}

\subsection{Convex Relaxation of the Flow Model}
To obtain a convex formulation, the flows are treated as independent decision variables and the $\min(\cdot)$ operators are replaced by inequality constraints:
\[
0 \le \bar{f}_{ij}(k) \le d_i(x_i(k)),
\]
\[
\sum_{j:\,j \to i} \bar{f}_{ji}(k) \le s_i(x_i(k)).
\]
\subsection{MPC Problem Statement : Finite-horizon optimal control problem}
A prediction horizon of length $K$ is considered, together with the following cost function:
\[
J = \sum_{k=0}^{K} \left( \sum_{i \in \mathcal{X}} x_i(k) + \sum_{T \in \mathcal{T}} x_T(k) \right).
\]

The objective is to minimize $J$ subject to:
\begin{itemize}
    \item control constraints:
    \[
    0 \le u_T(k) \le x_T(k), \quad \gamma_i \in [0,1],
    \]
    \item flow constraints:
    \[
    \sum_{m:\,i \to m} \bar{f}_{im}(k) \le d_i(x_i(k)),
    \]
    \[
    \sum_{j:\,j \to i} \bar{f}_{ji}(k) \le s_i(x_i(k)),
    \]
    \[
    \sum_{j:\,T \to j} f_{Tj}(k) \le u_T(k)\, x_T(k),
    \]
    \item system dynamics,
    \item no return flows to tanks,
    \item given initial condition $x(0)$.
\end{itemize}
\subsection{Numerical Implementation of the MPC Controller}



\section{Coupling the MPC Controller with the Simulator}
\subsection{Simulator Description}

\subsection{Closed-Loop Control Architecture}
The control is done following these steps : 
\begin{itemize}
    \item Formulate the problem statement (cost function, constraints)
    \item Solve the optimization problem
    \item Apply the first half solutions of the optimization problem
    \item Go again with the previous steps until we get the last iteration
\end{itemize}

\subsection{Closed-Loop Simulation Results}
This part is fed with both the  MPC results and the simulator outputs based on the MPC solution.\\
The MPC is run over a finite horizon of 200 for 2000 iterations.


\begin{figure}[h]
    \centering
    \begin{subfigure}{0.49\linewidth}
        \centering
        \includegraphics[width=\linewidth]{Images/Mpc_tank_states_hor200_2000iter.png}
        \caption{MPC tank states}
        \label{fig:MPC_tank_states}
    \end{subfigure}
    \hfill
    \begin{subfigure}{0.49\linewidth}
        \centering
        \includegraphics[width=\linewidth]{Images/Simulator_tank_states_hor200_2000iter.png}
        \caption{Simulator tank states}
        \label{fig:simulator_tank_states}
    \end{subfigure}
    \caption{MPC and simulator tnak states}
    \label{fig:MPC_simulator_tank_states}
\end{figure}

\begin{figure}[h]
    \centering
    \includegraphics[width=0.7\linewidth]{Images/Mpc_cells_states_hor200_2000iter.png}
    \caption{MPC cells states}
    \label{fig:MPC_cells_states}
\end{figure}

\begin{figure}
    \centering
    \includegraphics[width=0.7\linewidth]{Images/Simulator_cells_states_hor200_2000iter.png}
    \caption{Simulator cells states}
    \label{fig:simulator_cells_states}
\end{figure}

\section{Results and Performance Analysis}
\subsection{Comparison Between Open-Loop and Closed-Loop Behavior}

Table~\ref{tab:tableComparisonOL_MPC} compares the time performance of different control strategies. The results show that Model Predictive Control (MPC) with a prediction horizon of 800 and the optimization-based approach lead to very similar outcomes, with times of 733 s and 732 s respectively, indicating nearly identical closed-loop behavior. In contrast, open-loop strategies result in significantly longer times. When the two tanks empty sequentially, the time increases to 902 s, while it reaches 836 s when both tanks empty simultaneously. These results clearly highlight the advantage of closed-loop control strategies, which provide a more efficient dynamic management of the system and significantly reduce the time required to reach the condition where only one vehicle remains in zone X6.
\begin{table}[H]
    \centering
    \begin{tabularx}{\textwidth}{|X|c|}
        \hline
        \rowcolor{gray!20}
        Used Method & Time (in seconds) when there are only one vehicle left in X6 \\ \hline
        MPC with horizon 800 & 733 \\ \hline
        Optimization problem & 732 \\ \hline
        Open-loop when the two tanks empty one after the other & 902 \\ \hline
        Open-loop when both tanks empty at the same time & 836 \\ \hline
    \end{tabularx}
    \caption{Comparison Between Open-Loop and Closed-Loop Behavior}
    \label{tab:tableComparisonOL_MPC}
\end{table}

\subsection{Impact of MPC on Congestion Reduction}
\subsection{Discussion on Model-Simulator Mismatch}

\section{Conclusion and Perspectives}
\begin{itemize}
    \item Summary of the main results
    \item Limitations of the proposed approach
    \item Perspectives and future work
\end{itemize}

\newpage
\listoffigures



\end{document}
